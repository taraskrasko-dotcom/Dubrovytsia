# 🚨 ШВИДКЕ ВИРІШЕННЯ: Фото не додаються на GitHub Pages

## ✅ ЩО РОБИТИ ЗАРАЗ:

### Крок 1: Перевірка Apps Script
1. Відкрий свій Google Apps Script
2. **Розгорнути** → **Керування розгортаннями**
3. Знайди своє розгортання
4. Натисни "олівець" (редагувати)
5. **КРИТИЧНО**: У полі "Хто має доступ" має бути **"Усі"** (не "Лише я"!)
6. Натисни "Оновити"
7. Скопіюй адресу

### Крок 2: Оновлення коду
Замість старого коду в Apps Script вставляй цей:

```javascript
function doPost(e) {
  try {
    var sheet = SpreadsheetApp.getActiveSpreadsheet().getSheetByName('Аркуш1');
    var data = JSON.parse(e.postData.contents);
    
    if (data.action === 'addPhoto') {
      sheet.appendRow([
        data.imageUrl,
        data.title,
        data.period,
        data.date || new Date().toLocaleDateString()
      ]);
      
      return ContentService
        .createTextOutput(JSON.stringify({status: 'success'}))
        .setMimeType(ContentService.MimeType.JSON);
    }
  } catch (error) {
    return ContentService
      .createTextOutput(JSON.stringify({status: 'error', message: error.toString()}))
      .setMimeType(ContentService.MimeType.JSON);
  }
}

function doGet(e) {
  try {
    var sheet = SpreadsheetApp.getActiveSpreadsheet().getSheetByName('Аркуш1');
    
    if (e.parameter.action === 'getPhotos') {
      var data = sheet.getDataRange().getValues();
      var photos = [];
      
      for (var i = 1; i < data.length; i++) {
        if (data[i][0]) {
          photos.push({
            imageUrl: data[i][0],
            title: data[i][1],
            period: data[i][2],
            date: data[i][3]
          });
        }
      }
      
      return ContentService
        .createTextOutput(JSON.stringify({status: 'success', photos: photos}))
        .setMimeType(ContentService.MimeType.JSON);
    }
  } catch (error) {
    return ContentService
      .createTextOutput(JSON.stringify({status: 'error', message: error.toString()}))
      .setMimeType(ContentService.MimeType.JSON);
  }
}
```

### Крок 3: Оновлення script.js
1. Завантаж **НОВИЙ** script.js (з цієї папки)
2. Заміни старий на GitHub
3. Знайди **2 місця** з `GOOGLE_SCRIPT_URL`
4. Вставляй СВОЮ адресу з Apps Script

### Крок 4: Тестування
1. Відкрий `test-connection.html` у браузері
2. Вставляй свою адресу
3. Тестуй всі кнопки
4. Якщо все зелене (✅) - працює!

---

## 🔍 ДІАГНОСТИКА

### Де подивитись помилки:
1. Відкрий сайт
2. Натисни **F12**
3. Вкладка **Console**
4. Спробуй додати фото
5. Читай що пише в консолі

### Типові помилки:

**❌ "CORS error"**
→ У Apps Script не вибрано "Усі"
→ Виправ: Apps Script → Керування розгортаннями → Усі

**❌ "404 Not Found"**
→ Неправильна адреса
→ Виправ: Скопіюй адресу заново з Apps Script

**❌ "Failed to fetch"**
→ Немає інтернету або блокування
→ Виправ: Перевір інтернет, спробуй з іншого браузера

---

## 📋 ФАЙЛИ В ПАПЦІ:

- **script.js** - оновлений JavaScript (заміни старий на цей)
- **test-connection.html** - тестування з'єднання
- **ВИРІШЕННЯ_ПРОБЛЕМИ.txt** - детальна інструкція
- **КОД_ДЛЯ_GOOGLE_APPS_SCRIPT.js** - код для Apps Script

---

## 🎯 ШВИДКИЙ ЧЕКЛИСТ:

- [ ] Google Таблиця створена
- [ ] Заголовки: imageUrl, title, period, date
- [ ] Apps Script код вставлений
- [ ] Створено розгортання з доступом "Усі"
- [ ] Адреса скопійована повністю (/exec в кінці)
- [ ] Адреса вставлена в script.js (2 місця!)
- [ ] script.js завантажений на GitHub
- [ ] Кеш очищений (Ctrl+Shift+Delete)
- [ ] test-connection.html показує зелені галочки

---

## 💡 АЛЬТЕРНАТИВНИЙ МЕТОД (якщо нічого не допомагає):

Додавай фото **вручну в Google Таблицю**:
1. Відкрий Google Таблицю
2. В новий рядок вставляй:
   - A: `https://i.imgur.com/твоє-фото.jpg`
   - B: `Назва фото`
   - C: `before-1900` або `1900-1945` або `after-1945`
   - D: `29.01.2026`
3. Оновлюй сторінку photo-archive.html
4. Фото з'явиться!

Це не так зручно, але **100% працює**.

---

## 🆘 ПОТРІБНА ДОПОМОГА?

1. Запусти `test-connection.html`
2. Зроби скріншоти тестів
3. Відкрий консоль (F12)
4. Зроби скріншот помилок

Успіхів! 🚀
